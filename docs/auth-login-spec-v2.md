# Auth 登录模块设计规格 v2

> 更新日期: 2026-02-02
> 需求来源: Pleamon 通过 Alex 同步

---

## 核心变更点

### 1. 登录方式（第一版必须支持）
- ✅ **账号密码** - 现有
- ✅ **TOTP** - 内联验证
- ✅ **Passkey** - 内联验证
- ❌ **SSO** - 本次不做

### 2. MFA 流程 - 内联实现（重要！）
**禁止跳转独立页面**，所有 MFA 步骤在右侧表单区域内完成：
```
┌─────────────────┬─────────────────────────────┐
│                 │                             │
│   BrandPanel    │   [Step 1: 账号密码表单]     │
│   (左侧已确认)   │                             │
│                 │   ─────────────────────     │
│                 │   [Step 2: MFA 选择器]       │
│                 │   - TOTP                    │
│                 │   - Passkey                 │
│                 │   - 备用码                   │
│                 │                             │
│                 │   ─────────────────────     │
│                 │   [Step 3: 具体验证]         │
│                 │   - OTP 输入框               │
│                 │   - Passkey 脉冲动画         │
│                 │   - 备用码输入               │
│                 │                             │
└─────────────────┴─────────────────────────────┘
```

### 3. 视觉层级
- **左侧**: BrandPanel 风格已确认 ✅
- **右侧**: 需要优化 ⚠️

---

## 组件架构

### 登录页面状态机
```typescript
type LoginStep = 
  | 'credentials'    // 输入账号密码
  | 'mfa-select'     // 选择 MFA 方式
  | 'mfa-totp'       // TOTP 验证
  | 'mfa-passkey'    // Passkey 验证
  | 'mfa-recovery'   // 备用码验证
  | 'success';       // 验证成功
```

### 状态流转
```
credentials ──[登录成功需MFA]──> mfa-select
    │                               │
    │                        [选择方式]
    │                               │
    │          ┌────────────────────┼────────────────────┐
    │          │                    │                    │
    │          ▼                    ▼                    ▼
    │    mfa-totp             mfa-passkey         mfa-recovery
    │          │                    │                    │
    │          └────────────────────┴────────────────────┘
    │                               │
    │                          [验证成功]
    │                               │
    └───────────────────────────────┘
```

---

## 左侧 BrandPanel（已确认）

### 视觉风格参考
- 深色渐变网格背景 (`#0F0B2E`)
- 浮动光球动画 (CSS `@keyframes float`)
- 网格叠加 + 噪点纹理
- 玻璃态特性卡片
- 数据统计行

### 响应式
- `lg` (1024px+): 显示，宽度 50%
- `xl` (1440px+): 宽度 55%

### 文件位置
`apps/admin/src/components/auth/BrandPanel.tsx` ✅ 已确认无需改动

---

## 右侧表单区域（需优化）

### Step 1: 账号密码登录

```
┌─────────────────────────────────┐
│  👋 欢迎回来                     │
│  登录您的账户                    │
│  输入凭据以访问支付管理平台       │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │  🔑 Passkey 快捷登录     │   │  ← 保留
│  └─────────────────────────┘   │
│                                 │
│  ───────── 或 ─────────         │
│                                 │
│  用户名 / 邮箱 [____________]   │
│  密码          [____________]   │
│                                 │
│  ☑️ 记住此设备    忘记密码？     │
│                                 │
│  ┌─────────────────────────┐   │
│  │        登 录            │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

**变更点：**
- ❌ 移除 "SSO 登录" 按钮
- ✅ 保留 "Passkey 快捷登录" 作为主入口

### Step 2: MFA 方式选择

```
┌─────────────────────────────────┐
│  🔐 双因素验证                   │
│  请选择验证方式完成身份验证       │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │ 📱 身份验证器 (TOTP)     │   │  ← 默认选中
│  │    使用 Google/Microsoft │   │
│  │    Authenticator         │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 🔷 安全密钥 (Passkey)    │   │
│  │    使用 USB 密钥或        │   │
│  │    指纹识别               │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 📝 备用验证码            │   │
│  │    使用一次性备用码      │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │        继 续            │   │
│  └─────────────────────────┘   │
│                                 │
│        ← 返回登录              │
│                                 │
└─────────────────────────────────┘
```

### Step 3a: TOTP 验证

```
┌─────────────────────────────────┐
│  📱 身份验证器验证               │
│  请输入身份验证器中的 6 位验证码  │
├─────────────────────────────────┤
│                                 │
│        [◯][◯][◯]               │
│        [◯][◯][◯]               │
│                                 │
│   ⏱️ 30s 后刷新                  │
│                                 │
│  ☑️ 信任此设备（7天免验证）       │
│                                 │
│  ┌─────────────────────────┐   │
│  │        验 证            │   │
│  └─────────────────────────┘   │
│                                 │
│        ← 返回选择验证方式       │
│                                 │
└─────────────────────────────────┘
```

### Step 3b: Passkey 验证

```
┌─────────────────────────────────┐
│                                 │
│        ┌───────────┐           │
│        │  🔷 脉动   │           │  ← PasskeyPulse 组件
│        │   动画    │           │
│        └───────────┘           │
│                                 │
│  🔷 安全密钥验证                 │
│  请在弹出提示时触摸安全密钥      │
│  或使用指纹/面容识别             │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │    使用安全密钥验证      │   │
│  └─────────────────────────┘   │
│                                 │
│        ← 返回选择验证方式       │
│                                 │
└─────────────────────────────────┘
```

### Step 3c: 备用码验证

```
┌─────────────────────────────────┐
│  📝 使用备用码                   │
│  请输入您保存的 8 位备用验证码    │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │  XXXX-XXXX              │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │        验 证            │   │
│  └─────────────────────────┘   │
│                                 │
│        ← 返回选择验证方式       │
│                                 │
└─────────────────────────────────┘
```

---

## 复用组件清单

| 组件 | 位置 | 用途 |
|------|------|------|
| `BrandPanel` | `components/auth/BrandPanel.tsx` | 左侧品牌面板 ✅ |
| `OtpInput` | `components/auth/OtpInput.tsx` | 6位验证码输入 |
| `MfaMethodSelector` | `components/auth/MfaMethodSelector.tsx` | MFA 方式选择 |
| `PasskeyPulse` | `components/auth/PasskeyPulse.tsx` | Passkey 等待动画 |
| `SuccessOverlay` | `components/auth/SuccessOverlay.tsx` | 成功过渡动画 |
| `ErrorAlert` | `components/auth/ErrorAlert.tsx` | 错误提示 |

---

## 文件变更

### 修改
- `apps/admin/src/routes/login.tsx` - 重构为内联 MFA 流程

### 保留（可复用现有逻辑）
- `apps/admin/src/routes/mfa/verify.tsx` - 保留作为独立页面备用
- `apps/admin/src/routes/mfa/setup.tsx` - MFA 设置（首次启用）

---

## 技术要点

### 1. 会话状态管理
```typescript
// 登录响应包含 session_id，用于 MFA 流程
interface LoginResponse {
  session_id: string;           // 关键：用于 MFA 验证
  mfa_status: 'requires_verification' | 'verified';
  available_mfa_types: ('totp' | 'passkey' | 'recovery')[];
}
```

### 2. 状态持久化
- `sessionStorage.setItem('psp_session_id', data.session_id)` - 用于 MFA 验证
- MFA 完成后再清除 session_id

### 3. 表单验证
- 账号密码：同步验证
- TOTP: 6位数字，自动提交
- 备用码: 8位字符，支持 `-` 分隔

---

## 设计 Token

### 颜色
```typescript
brandColors = {
  primary: '#6366f1',
  primaryLight: 'rgba(99, 102, 241, 0.08)',
  gradient: 'linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%)',
}
```

### 阴影
```
card: 0 10px 40px rgba(0, 0, 0, 0.08), 0 2px 8px rgba(0, 0, 0, 0.04)
button-hover: 0 6px 20px rgba(99, 102, 241, 0.45)
```

### 圆角
```
card: 20px
button: 12px
input: 10px
```

---

## 动画效果

### 步骤切换
- 使用 `AnimatePresence` 或 CSS transition
- 方向：从右向左滑入新步骤
- 时长：300ms
- 缓动：ease-out

### Passkey 脉冲
- 现有 `PasskeyPulse` 组件
- 蓝色渐变圆环扩散动画

---

## 响应式

| 断点 | 左侧 BrandPanel | 右侧表单 |
|------|----------------|----------|
| `< 1024px` | 隐藏 | 100% 宽度 |
| `≥ 1024px` | 50% | 50% |
| `≥ 1440px` | 55% | 45% |

---

## 待确认事项

1. **备用码格式**: 是否保留 `XXXX-XXXX` 格式？
2. **信任设备时长**: 确认 7 天免验证
3. **Passkey 超时**: 等待用户操作超时时间？

---

## 左侧 BrandPanel 代码引用

如需查看已确认的左侧风格，参考:
```
apps/admin/src/components/auth/BrandPanel.tsx
```

关键视觉元素:
- 背景: `#0F0B2E` + 渐变网格
- 浮动光球: 3 个不同颜色的 `radial-gradient` + `blur()`
- 网格叠加: `linear-gradient` 模拟网格线
- 噪点纹理: SVG feTurbulence
- 玻璃卡片: `backdrop-filter: blur(8px)` + 半透明白色背景
